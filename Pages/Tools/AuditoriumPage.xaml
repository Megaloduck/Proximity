<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proximity.Pages.Tools.AuditoriumPage"
           Title="Auditorium">

    <VerticalStackLayout Padding="40" Spacing="20" VerticalOptions="Center">
        <Label Text="🎤"
               FontSize="64"
               HorizontalOptions="Center"/>

        <Label Text="Voice Auditorium"
               FontSize="28"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               TextColor="{DynamicResource PrimaryTextColor}"/>

        <Label Text="Multi-user voice rooms with audio mixing"
               FontSize="16"
               HorizontalOptions="Center"
               TextColor="{DynamicResource SecondaryTextColor}"/>

        <Label Text="Coming Soon in v1.3"
               FontSize="14"
               HorizontalOptions="Center"
               Margin="0,20,0,0"
               TextColor="{DynamicResource AccentColor}"/>
    </VerticalStackLayout>
</ContentPage>